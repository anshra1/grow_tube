/// Defines the shape of the App button.
enum AppButtonShape {
  /// Fully rounded corners (Stadium border).
  pill,

  /// Slightly rounded corners (4px), looking mostly rectangular.
  rectangular,

  /// Standard rounded corners (16px).
  rounded,
}
